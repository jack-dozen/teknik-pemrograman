#include <stdio.h>

int sum(int startNum, int endNum){
	int count, sum = 0;
	for (count = startNum; count <= endNum; count++)
		sum += count;
	return sum;
}

int thread(int threadNum, int endNum){
	int result;
	int startNum
	
	
	return result;
}

int main(){
	int endNum, threadNum;

	printf("JML_THREAD: ");
	scanf("%d", &threadNum);
	
	printf("END_NUM: ");
	scanf("%d", &endNum);
	
	return 0;
}
